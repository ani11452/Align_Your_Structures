dataset:  
  type: 'trajectory'
  dataset: "DRUGS"
  train_traj_dir: "${MD_DATA_ROOT}/output_trajectories/GEOM-DRUGS/4fs_HMR15_5ns_actual/train"
  val_traj_dir: "${MD_DATA_ROOT}/output_trajectories/GEOM-DRUGS/4fs_HMR15_5ns_actual/val"
  test_traj_dir: "${MD_DATA_ROOT}/output_trajectories/GEOM-DRUGS/4fs_HMR15_5ns_actual/test"
  train_conf_path: "${MD_DATA_ROOT}/processed_input_data/GEOM-DRUGS/GEOM-DRUGS_Train.pkl"
  val_conf_path: "${MD_DATA_ROOT}/processed_input_data/GEOM-DRUGS/GEOM-DRUGS_Val.pkl"
  test_conf_path: "${MD_DATA_ROOT}/processed_input_data/GEOM-DRUGS/GEOM-DRUGS_Test_Actual_compat.pkl"
  features: ['aromatic', 'hybridization', 'partial_charge', 'implicit_valence', 'degree', 'formal_charge', 'ring_size', 'chirality']
  transforms: ['edge_order|3']
  remove_hs: true
  filter_data: true
  expected_time_dim: 2
  num_frames: 26
  frame_rate: 13
  start_frame: 
  random_frame: true
  seed: 42
  subsample: 0.25
  
denoiser:
  type: 'egtn'
  n_layers: 6
  node_dim: 30
  ft_dim: 42
  edge_dim: 4
  hidden_dim: 128
  time_emb_dim: 32
  act_fn: 'silu'
  scale: 1
  pre_norm: false
  # conditioning: 'unconditional_forward'
  conditioning: 'unconditional_forward_autoreg'
  pretrain_ckpt:

diffusion:
  beta_schedule_name: 'linear'
  num_timesteps: 1000
  use_kabsch_alignment: true

optim:
  lr: 1.0e-4
  weight_decay: 0

train:
  max_epochs: 1000
  batch_size: 64
  accum_grad: 1
  resume_from_checkpoint:

eval:
  batch_size: 64
  num_sample_batches: 1
  samples_output_dir: "./vis_samples"

test:
  batch_size: 256
  # subsample:
  subsample: model_outputs_official/drugs_trajectory_official/drugs_noH_1000_kabsch_traj_interpolator_pretrain_fs_25/drugs_test_subset.pkl

wandb:
  entity: conf_traj_modeling
  project: drugs_trajectory_official
  name: drugs_noH_1000_kabsch_traj_egtn_autoregressive_2
